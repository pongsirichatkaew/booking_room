version: '3'

services:
    meeting_room_backend_dev:
        image: meeting_room_backend_dev
        ports:
            - "7777:7777"
        restart: always
        volumes:
            - ./:/code
        networks:
            - code-network-dev
networks:
    code-network-dev:
        driver: bridge
